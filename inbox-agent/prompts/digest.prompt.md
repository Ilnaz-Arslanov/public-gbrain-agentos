Below are compiled inbox entries from the last 26 hours. Write a daily digest
in the user's language (default English; if Russian materials dominate, use
Russian).

Format strictly as Telegram HTML:

```
<b>Inbox digest — YYYY-MM-DD</b>

<b>Type</b>
<a href="URL">title</a>: 1-sentence TL;DR.
<a href="URL">title</a>: 1-sentence TL;DR.

<b>Type</b>
...

<b>Themes</b>
- Theme 1.
- Theme 2.
- Theme 3.
```

Rules:
- Group entries by `type` (youtube, twitter, instagram, web, text).
- One line per entry, ≤140 chars.
- Use `<b>`, `<i>`, `<a>`, `<pre>` only. No markdown tables. No emoji.
- Total output ≤ 3500 characters.
- Open with `<b>Inbox digest — DATE</b>` followed by a blank line.
- End with a `<b>Themes</b>` block listing 2-3 cross-cutting themes.

If there are zero entries, output the single line `<i>No new items.</i>`.
